# .bash_profile
# Executed by bash for login shells (macOS terminals, SSH, FreeBSD console).
# Delegates to .bashrc which holds all interactive shell configuration.
# This ensures the same config loads for both login and non-login shells.

[ -r ~/.bashrc ] && source ~/.bashrc
